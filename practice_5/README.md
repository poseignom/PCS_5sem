# Практическое занятие №5
## Работа со списками. Передача данных между модулями

**ФИО:** Викторов Александр Дмитриевич  
**Группа:** ЭФБО-06-23 

### Цели занятия:
- Научиться отображать коллекции данных с помощью ListView.builder
- Освоить базовую навигацию Navigator.push / Navigator.pop и передачу данных через конструктор
- Научиться добавлять, редактировать и удалять элементы списка без внешних пакетов и сложных архитектур

### Выполненные шаги:

1. **Создание проекта и структуры**
   - Создан новый Flutter проект: `simple_notes_viktorov`
   - Организована структура папок: `models`, `screens`

2. **Модель данных**
   - Создан класс `Note` с полями: id, title, body
   - Реализован метод `copyWith` для удобного обновления объектов

3. **Главный экран**
   - Реализован список заметок с использованием ListView.builder
   - Добавлены карточки (Card) для красивого отображения
   - Реализовано пустое состояние при отсутствии заметок

4. **Экран редактирования**
   - Создан экран для добавления и редактирования заметок
   - Использована форма (Form) с валидацией
   - Реализовано сохранение данных

5. **Навигация и передача данных**
   - Настроена навигация между экранами с помощью Navigator
   - Реализована передача данных через конструктор
   - Организован возврат результатов между экранами

### Использованные технологии:

- **ListView.builder** - для эффективного отображения списков
- **StatefulWidget** - для управления состоянием
- **Navigator** - для навигации между экранами
- **Form и TextFormField** - для ввода и валидации данных
- **Card и ListTile** - для красивого UI
- **SnackBar** - для уведомлений пользователя

### Скриншоты:

Главный экран со списком заметок main.jpg

Экран добавления новой заметки dobav.jpg

Экран редактирования существующей заметки redact.jpg

Процесс удаления заметки manage.jpg

### Выводы:

Освоена работа со списками в Flutter с использованием ListView.builder. Реализована полнофункциональная система управления заметками с возможностями добавления, редактирования и удаления. Настроена навигация между экранами с передачей данных. Приложение демонстрирует основные принципы работы с коллекциями данных и управления состоянием в мобильных приложениях.

### Ключевые элементы кода:

```dart
// Создание списка с ListView.builder
ListView.builder(
  itemCount: _notes.length,
  itemBuilder: (context, index) {
    final note = _notes[index];
    return Card(
      child: ListTile(
        title: Text(note.title),
        subtitle: Text(note.body),
        onTap: () => _editNote(note),
      ),
    );
  },
)

// Навигация с передачей данных
final newNote = await Navigator.push<Note>(
  context,
  MaterialPageRoute(builder: (_) => const EditNotePage()),
);

// Возврат данных из экрана редактирования
Navigator.pop(context, result);